{
  "name": "flywire-brain",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "data": "bash scripts/download.sh && uv run --with duckdb python scripts/neuropil_counts.py && uv run --with pandas --with pyarrow --with numpy --with scikit-learn --with umap-learn python scripts/build_data.py && uv run --with pandas --with pyarrow --with numpy --with scipy --with scikit-learn --with umap-learn python scripts/build_connectome.py && uv run --with numpy --with pandas --with scipy --with scikit-learn --with igraph --with leidenalg --with infomap python scripts/build_clusters.py"
  },
  "dependencies": {
    "three": "^0.186.0"
  },
  "devDependencies": {
    "@types/three": "^0.186.0",
    "typescript": "^7.0.2",
    "vite": "^8.3.0"
  }
}
